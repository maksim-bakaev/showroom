<!DOCTYPE HTML>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
<title>Masonry — JQuery плагин для динамического отображения контента</title>

  <%= stylesheet_link_tag    "application_dash" %>
  <%= javascript_include_tag "application_dash" %>


  <style type="text/css">
    body {}
	.item-mas {
		float:left /* ОБЯЗАТЕЛЬНО задаем блокам float */; 
		width:19%; /*min-height:280px; */
		margin:0.5%;
		/*background:#333; color:#fff; text-align:center;*/
		
		/* параметры анимации */
-webkit-transition-duration: 0.5s;
   -moz-transition-duration: 0.5s;
	 -o-transition-duration: 0.5s;
		transition-duration: 0.5s;
		}	
		
	.tall {}
  </style>
  
<script type="text/javascript">
  $(document).ready(function(){ 
	$('#container').masonry({ // выбираем элемент-контейнер в котором расположены блоки для динамической верстки
	  itemSelector: '.item-mas', // указываем класс элемента являющегося блоком в нашей сетке
	  singleMode: true, // true - если у вас все блоки одинаковой ширины
	  resizeable: true, // перестраивает блоки при изменении размеров окна
	  isAnimated: !Modernizr.csstransitions // анимируем перестроение блоков при помощи css transitions и Modernizr
	}); 
  });
</script>

</head>

<body>
  <%=render 'layouts/head' %>
  <%= yield %>
  
  <!-- Modal -->
  <div id="myModal" class="modal hide fade higherWider" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h3 id="myModalLabel">20, 40 или 60 сеансов коррекции фигуры в течение полугода в Тонус-клубе на Подоле. Сертификаты на 6 месяцев!</h3>
    </div>
    <div class="modal-body">
      <%=render 'dashboard/test/show' %>
    </div>
    <div class="modal-footer">
      <button class="btn btn-primary btn-block">Купить</button>
    </div>
  </div>
  <!-- /Modal -->
  
</body>
</html>

